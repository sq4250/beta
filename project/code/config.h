/**
 * config.h — 可调参数 (上车标定/调参修改此文件)
 */

#ifndef CONFIG_H
#define CONFIG_H

#include "common.h"

//===================================================车体几何===================================================
#define WHEELBASE         0.15f       // 轴距 L [m]
#define INV_WHEELBASE     6.6666667f  // 1/WHEELBASE
#define TRACK_WIDTH       0.15f       // 后轮距 [m]
//===================================================车体几何===================================================

//===================================================编码器标定===================================================
#define ENCODER_SCALE     0.0001f     // 编码器差值→线速度 [m/s per count/s]
//===================================================编码器标定===================================================

//===================================================转向===================================================
#define INV_STEERING_RATIO  0.5f       // 前轮→舵机: srad = delta * INV (舵机1°↔前轮2°)
//===================================================转向===================================================

//===================================================IMU 标定===================================================
#define IMU_BIAS_TOTAL      400       // 零偏采样总数
#define IMU_BIAS_FAST       200       // 前N样本递推平均
#define IMU_BIAS_ALPHA      0.005f    // 后续 EMA 系数
#define IMU_ACC_NORM_MIN    0.9f      // 运动拒绝: 加速度下限 [g]
#define IMU_ACC_NORM_MAX    1.1f      // 运动拒绝: 加速度上限 [g]
#define IMU_MOTION_THR      500       // 运动拒绝: 陀螺阈值 [raw]
#define IMU_YAW_ALPHA       0.95f     // 互补滤波: 陀螺权重
#define IMU_QUAT_ALPHA      0.05f     // 互补滤波: 四元数权重
//===================================================IMU 标定===================================================

//===================================================控制器频率===================================================
#define TRACKER_FREQ       200        // LQR+PWM 频率 Hz (1k/5)
#define CTRL_DT            0.005f     // = 1/TRACKER_FREQ (5ms)
#define ISR_DT             0.001f     // 1kHz PIT 周期
//===================================================控制器频率===================================================

//===================================================航点===================================================
#define TOL_XY             0.05f      // 到达容差 [m]
#define MAX_WAYPOINTS      16         // 最大航点数
//===================================================航点===================================================

//===================================================LQR===================================================
#define LQR_V_MIN          0.1f       // 查表最小速度 [m/s]
//===================================================LQR===================================================

//===================================================纵向 LADRC===================================================
#define LONG_B0            7.0f       // 控制增益 (稳态 v=b0/alpha≈3.5m/s)
#define LONG_ALPHA         2.0f       // 速度衰减系数
#define LONG_WO            15.0f      // LESO 观测带宽 [rad/s]
#define LONG_KP            8.0f       // 位置误差 P
#define LONG_KD            6.0f       // 速度误差 D
#define LONG_DT            CTRL_DT    // 控制周期
//===================================================纵向 LADRC===================================================

//===================================================3-clamp===================================================
#define A_LONG_MAX          4.0f      // 纵向加速度上限 [m/s²]
#define OMEGA_DELTA_MAX    14.0f      // 前轮转角角速度上限 [rad/s]
#define V_MAX               3.5f      // 速度上限 [m/s]
#define DELTA_MAX           0.4636f   // 前轮转角上限 [rad] (≈26.5°)
//===================================================3-clamp===================================================

//===================================================速度滤波===================================================
#define SPEED_FILT_ALPHA    0.1f       // 编码器速度 EMA 系数 (1kHz, ≈10Hz 截止)
//===================================================速度滤波===================================================

#endif
